package iamrescue.communication.messages.codec.property;


public abstract class APropertyCodec implements PropertyCodec {

	// protected abstract int encode(Entity object, Property propertyValue);
	//
	// public synchronized void encode(Entity object, Property propertyValue,
	// BitStreamEncoder encoder) {
	// int numberValue = encode(object, propertyValue);
	// encoder.appendNumber(numberValue);
	// }
}
